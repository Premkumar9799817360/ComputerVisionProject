version: "3.11"

services:
  sightflow:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env                        # your existing .env file — never commit this
    volumes:
      - ./reports:/app/reports      # PDF reports persist on your local machine
    restart: unless-stopped
